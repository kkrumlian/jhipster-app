angular.module('PP-admin', [
        'PP-admin-metrics',
        'PP-admin-audits',
        'PP-admin-logs',
        'PP-admin-docs'
	])

    .controller('AdminController', function ($scope) {
    });
